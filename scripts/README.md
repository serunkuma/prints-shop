# Scripts

This folder stores project-specific scripts and automations that emerge as the project grows — build helpers, data migration tools, validation runners, and similar. Add scripts here as workflows become repetitive enough to automate.
